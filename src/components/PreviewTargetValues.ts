import {createElement} from 'react'
import type {LivePrescription} from '../engine/terrainModifier.ts'
import type {NoFtpPresentation} from '../engine/release4024.ts'
const stat=(label:string,value:string)=>createElement('span',{className:'preview-stat',key:label},createElement('small',null,label),createElement('strong',null,value))
export function PreviewTargetValues({target}:{target:LivePrescription|NoFtpPresentation}){if('mode'in target)return createElement('div',{className:'preview-target-values no-ftp-preview'},stat('EFFORT',target.effort),stat('CADENCE',target.cadence),stat('RESISTANCE',target.resistance));const manual=target.manualTarget,start=manual.recommendedResistance===null?'Unavailable':`${manual.recommendedResistance}% @ ${manual.recommendedCadence} rpm`;return createElement('div',{className:'preview-target-values'},stat('ZONE',target.zone),stat('POWER',target.power),stat('CADENCE',target.cadence),stat('RESISTANCE',target.resistance),stat('START',start),stat('ADJUSTMENT',manual.adjustmentGuidance))}
