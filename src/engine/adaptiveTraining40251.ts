import type { Alpha4025State, CalendarAssignment, OutdoorActivity, ReadinessProjection, TrainingPlan } from './alpha4025.ts'

export const WORKOUT_METADATA_VERSION='rtr-curated-2026.09.1'
export type TrainingZone='Recovery'|'Endurance'|'Tempo'|'Sweet Spot'|'Threshold'|'VO2 Max'|'Anaerobic'|'Cadence and technique'|'Sustained climbing'
export type DisciplineFocus='Endurance'|'Sustained climbing'|'FTP development'|'VO2 development'|'Cadence and technique'|'Recovery'|'Outdoor endurance'
export type CuratedWorkout={id:string;title:string;primaryZone:TrainingZone;secondaryZone?:TrainingZone;purpose:string;discipline:DisciplineFocus;durationMinutes:number;workIntervalMinutes:number;recoveryIntervalMinutes:number;repetitions:number;ftpRange?:[number,number];unknownFtpEffort:string;cadence:[number,number];resistanceGuidance:string;demand:1|2|3|4|5;recoveryCost:1|2|3|4|5;environment:'INDOOR'|'OUTDOOR'|'EITHER';requiredEquipment:string[];difficulty:number;alternatives:{easier:string[];harder:string[];shorter:string[];longer:string[];indoor:string[];outdoor:string[];recovery:string[]};fueling:'Recovery'|'Endurance'|'Quality'|'Long ride'}
const alternatives=(easier:string[],harder:string[],shorter:string[],longer:string[],indoor:string[],outdoor:string[],recovery=['recovery-spin-30'])=>({easier,harder,shorter,longer,indoor,outdoor,recovery})
export const CURATED_OFFSEASON_WORKOUTS:CuratedWorkout[]=[
 {id:'recovery-spin-30',title:'Recovery Spin 30',primaryZone:'Recovery',purpose:'Promote easy movement without adding meaningful fatigue.',discipline:'Recovery',durationMinutes:30,workIntervalMinutes:0,recoveryIntervalMinutes:30,repetitions:1,ftpRange:[.45,.55],unknownFtpEffort:'Very easy; full conversation',cadence:[80,95],resistanceGuidance:'Light, supported resistance only',demand:1,recoveryCost:1,environment:'EITHER',requiredEquipment:['Bicycle or indoor bike'],difficulty:1,alternatives:alternatives([],['endurance-steady-45'],[],['endurance-steady-45'],['recovery-spin-30'],['outdoor-easy-60'],[]),fueling:'Recovery'},
 {id:'endurance-steady-45',title:'Steady Endurance 45',primaryZone:'Endurance',secondaryZone:'Cadence and technique',purpose:'Build aerobic consistency and economical pedaling.',discipline:'Endurance',durationMinutes:45,workIntervalMinutes:35,recoveryIntervalMinutes:10,repetitions:1,ftpRange:[.56,.72],unknownFtpEffort:'Easy conversational effort',cadence:[82,96],resistanceGuidance:'Comfortable steady load',demand:2,recoveryCost:2,environment:'EITHER',requiredEquipment:['Bicycle or indoor bike'],difficulty:2.5,alternatives:alternatives(['recovery-spin-30'],['endurance-steady-75'],['recovery-spin-30'],['endurance-steady-75'],['endurance-steady-45'],['outdoor-easy-60']),fueling:'Endurance'},
 {id:'endurance-steady-75',title:'Steady Endurance 75',primaryZone:'Endurance',secondaryZone:'Tempo',purpose:'Extend durable aerobic work without threshold load.',discipline:'Endurance',durationMinutes:75,workIntervalMinutes:60,recoveryIntervalMinutes:15,repetitions:1,ftpRange:[.58,.75],unknownFtpEffort:'Easy to comfortable conversation',cadence:[80,94],resistanceGuidance:'Steady endurance load',demand:3,recoveryCost:3,environment:'EITHER',requiredEquipment:['Bicycle or indoor bike'],difficulty:4,alternatives:alternatives(['endurance-steady-45'],['outdoor-endurance-120'],['endurance-steady-45'],['outdoor-endurance-120'],['endurance-steady-75'],['outdoor-easy-60']),fueling:'Endurance'},
 {id:'climbing-sustain-60',title:'Sustained Climbing 3 × 10',primaryZone:'Sustained climbing',secondaryZone:'Sweet Spot',purpose:'Develop controlled sustained climbing and low-variability pacing.',discipline:'Sustained climbing',durationMinutes:60,workIntervalMinutes:10,recoveryIntervalMinutes:5,repetitions:3,ftpRange:[.88,.96],unknownFtpEffort:'Hard but controlled',cadence:[68,82],resistanceGuidance:'Supported seated climbing load',demand:4,recoveryCost:4,environment:'INDOOR',requiredEquipment:['Indoor bike with resistance'],difficulty:5,alternatives:alternatives(['tempo-climb-45'],['threshold-4x8'],['tempo-climb-45'],['climbing-sustain-75'],['climbing-sustain-60'],['outdoor-endurance-120']),fueling:'Quality'},
 {id:'tempo-climb-45',title:'Controlled Climbing Tempo',primaryZone:'Tempo',secondaryZone:'Sustained climbing',purpose:'Preserve sustained-climbing purpose with reduced load.',discipline:'Sustained climbing',durationMinutes:45,workIntervalMinutes:8,recoveryIntervalMinutes:4,repetitions:3,ftpRange:[.76,.86],unknownFtpEffort:'Comfortable to controlled',cadence:[70,85],resistanceGuidance:'Moderate seated climbing load',demand:3,recoveryCost:2,environment:'INDOOR',requiredEquipment:['Indoor bike with resistance'],difficulty:3.5,alternatives:alternatives(['endurance-steady-45'],['climbing-sustain-60'],['recovery-spin-30'],['climbing-sustain-60'],['tempo-climb-45'],['outdoor-easy-60']),fueling:'Quality'},
 {id:'threshold-4x8',title:'Threshold 4 × 8',primaryZone:'Threshold',secondaryZone:'Sweet Spot',purpose:'Develop repeatable work near functional threshold.',discipline:'FTP development',durationMinutes:60,workIntervalMinutes:8,recoveryIntervalMinutes:4,repetitions:4,ftpRange:[.95,1.02],unknownFtpEffort:'Very hard but repeatable',cadence:[78,92],resistanceGuidance:'Firm stable load',demand:5,recoveryCost:5,environment:'INDOOR',requiredEquipment:['Indoor bike with resistance'],difficulty:6.5,alternatives:alternatives(['climbing-sustain-60'],['vo2-5x4'],['tempo-climb-45'],['climbing-sustain-75'],['threshold-4x8'],[]),fueling:'Quality'},
 {id:'vo2-5x4',title:'High Aerobic 5 × 4',primaryZone:'VO2 Max',purpose:'Develop controlled high-aerobic repeatability.',discipline:'VO2 development',durationMinutes:55,workIntervalMinutes:4,recoveryIntervalMinutes:4,repetitions:5,ftpRange:[1.06,1.16],unknownFtpEffort:'Very hard; controlled repetitions',cadence:[88,102],resistanceGuidance:'High-aerobic load without cadence collapse',demand:5,recoveryCost:5,environment:'INDOOR',requiredEquipment:['Indoor bike with resistance'],difficulty:7,alternatives:alternatives(['threshold-4x8'],[],['climbing-sustain-60'],[],['vo2-5x4'],[]),fueling:'Quality'},
 {id:'vo2-6x4-70',title:'High Aerobic 6 × 4',primaryZone:'VO2 Max',purpose:'Progress high-aerobic repeatability after successful preparation.',discipline:'VO2 development',durationMinutes:70,workIntervalMinutes:4,recoveryIntervalMinutes:4,repetitions:6,ftpRange:[1.06,1.16],unknownFtpEffort:'Very hard; controlled repetitions',cadence:[88,102],resistanceGuidance:'High-aerobic load without cadence collapse',demand:5,recoveryCost:5,environment:'INDOOR',requiredEquipment:['Indoor bike with resistance'],difficulty:8,alternatives:alternatives(['vo2-5x4'],[],['vo2-5x4'],[],['vo2-6x4-70'],[]),fueling:'Quality'},
 {id:'outdoor-easy-60',title:'Outdoor Endurance 60',primaryZone:'Endurance',secondaryZone:'Cadence and technique',purpose:'Develop outdoor pacing, handling and comfort.',discipline:'Outdoor endurance',durationMinutes:60,workIntervalMinutes:50,recoveryIntervalMinutes:10,repetitions:1,unknownFtpEffort:'Easy conversation; use heart rate and talk test',cadence:[75,95],resistanceGuidance:'Terrain-led gearing; no invented outdoor resistance',demand:2,recoveryCost:2,environment:'OUTDOOR',requiredEquipment:['Outdoor bicycle'],difficulty:2.5,alternatives:alternatives(['recovery-spin-30'],['outdoor-endurance-120'],['recovery-spin-30'],['outdoor-endurance-120'],['endurance-steady-45'],['outdoor-easy-60']),fueling:'Endurance'},
 {id:'outdoor-endurance-120',title:'Outdoor Endurance 120',primaryZone:'Endurance',secondaryZone:'Tempo',purpose:'Build long outdoor endurance, pacing, comfort and fueling.',discipline:'Outdoor endurance',durationMinutes:120,workIntervalMinutes:105,recoveryIntervalMinutes:15,repetitions:1,unknownFtpEffort:'Comfortable by talk test and heart rate',cadence:[72,94],resistanceGuidance:'Terrain-led gearing; no invented outdoor watts',demand:4,recoveryCost:4,environment:'OUTDOOR',requiredEquipment:['Outdoor bicycle'],difficulty:5,alternatives:alternatives(['outdoor-easy-60'],[],['outdoor-easy-60'],[],['endurance-steady-75'],['outdoor-endurance-120']),fueling:'Long ride'}]
export const workoutById=(id:string)=>CURATED_OFFSEASON_WORKOUTS.find(w=>w.id===id)??null

export type ZoneDevelopment={zone:TrainingZone;level:number|null;confidence:'None'|'Low'|'Moderate'|'High';lastEvidenceDate:string|null;evidenceSource:string;successfulWorkoutIds:string[];struggledWorkoutIds:string[];direction:'Building'|'Stable'|'Recovering'|'Insufficient evidence'}
export type DifficultyEvaluation={assignmentId:string;workoutId:string;label:'Recovery'|'On Target'|'Challenging'|'High Risk'|'Not Advised'|'Unknown — insufficient evidence';explanation:string;evaluatedAt:string}
export type AdaptationRecord={id:string;date:string;assignmentId:string;originalWorkoutId:string;replacementWorkoutId:string;trigger:string;evidence:string[];explanation:string;decision:'ACCEPTED'|'DECLINED'}
export type FourWeekPreview={generatedAt:string;fromWeek:number;totalRideMinutes:number;strengthSessions:number;qualitySessions:number;enduranceSessions:number;outdoorSessions:number;recoveryDays:number;recoveryWeeks:number;zoneExposure:Partial<Record<TrainingZone,number>>;sustainedClimbingSessions:number;consecutiveTrainingDays:number;highDemandDays:number;conflicts:string[];historyFingerprint:string}
export type CandidateResult={selected:CuratedWorkout|null;selectedReason:string;rejected:Array<{workout:CuratedWorkout;reason:string}>}
export type AdaptiveTrainingState={metadataVersion:string;zoneDevelopment:ZoneDevelopment[];difficultyEvaluations:DifficultyEvaluation[];candidateSelection:CandidateResult|null;fourWeekPreview:FourWeekPreview|null;adaptations:AdaptationRecord[];selectedAlternatives:Record<string,string>}
export const emptyAdaptiveTraining=():AdaptiveTrainingState=>({metadataVersion:WORKOUT_METADATA_VERSION,zoneDevelopment:[],difficultyEvaluations:[],candidateSelection:null,fourWeekPreview:null,adaptations:[],selectedAlternatives:{}})
const ALL_ZONES:TrainingZone[]=['Endurance','Tempo','Sweet Spot','Threshold','VO2 Max','Anaerobic','Sustained climbing','Cadence and technique']
export function buildZoneDevelopment(completions:Array<{workoutId:string;date:string;status:'SUCCESS'|'STRUGGLED'|'INCOMPLETE';source:string}>):ZoneDevelopment[]{return ALL_ZONES.map(zone=>{const valid=completions.filter(c=>workoutById(c.workoutId)?.primaryZone===zone||workoutById(c.workoutId)?.secondaryZone===zone),success=valid.filter(c=>c.status==='SUCCESS'),struggled=valid.filter(c=>c.status!=='SUCCESS'),last=valid.toSorted((a,b)=>b.date.localeCompare(a.date))[0];if(!last)return {zone,level:null,confidence:'None',lastEvidenceDate:null,evidenceSource:'No completed RtR workout evidence',successfulWorkoutIds:[],struggledWorkoutIds:[],direction:'Insufficient evidence'};const level=Math.max(1,Math.min(10,(success.at(-1)?workoutById(success.at(-1)!.workoutId)?.difficulty??1:1)-(struggled.length?1:0)));return {zone,level,confidence:valid.length>=4?'High':valid.length>=2?'Moderate':'Low',lastEvidenceDate:last.date,evidenceSource:last.source,successfulWorkoutIds:success.map(x=>x.workoutId),struggledWorkoutIds:struggled.map(x=>x.workoutId),direction:struggled.length?'Recovering':success.length>1?'Building':'Stable'}})}
export function evaluateDifficulty(assignmentId:string,workout:CuratedWorkout,development:ZoneDevelopment|undefined,date:string):DifficultyEvaluation{if(workout.primaryZone==='Recovery')return {assignmentId,workoutId:workout.id,label:'Recovery',explanation:'Recovery — this session is intentionally low demand.',evaluatedAt:date};if(!development?.level)return {assignmentId,workoutId:workout.id,label:'Unknown — insufficient evidence',explanation:`Unknown — RtR does not have enough completed ${workout.primaryZone} work to estimate this session confidently.`,evaluatedAt:date};const delta=workout.difficulty-development.level,label=delta<=1?'On Target':delta<=2?'Challenging':delta<=3?'High Risk':'Not Advised';return {assignmentId,workoutId:workout.id,label,explanation:`${label} — workout progression ${workout.difficulty.toFixed(1)} compared with the supported ${workout.primaryZone} level ${development.level.toFixed(1)}.`,evaluatedAt:date}}
export const SELECTION_WEIGHTS={purposeMatch:40,goalMatch:20,levelFit:15,durationFit:10,environmentFit:8,phaseFit:4,recoveryFit:3,workloadFit:3,proximityFit:2,outdoorReplacement:2} as const
export function selectCandidate(input:{purpose:string;goal:string;development:ZoneDevelopment[];availableMinutes:number;environment:'INDOOR'|'OUTDOOR'|'EITHER';equipment:string[];recoveryWeek:boolean;readiness:ReadinessProjection;previousHighIntensity:boolean;struggledWorkoutId?:string;weeklyLoadMinutes?:number;nextQualityDays?:number;recentLongOutdoor?:boolean}):CandidateResult{const rejected:Array<{workout:CuratedWorkout;reason:string}>=[],valid=CURATED_OFFSEASON_WORKOUTS.filter(w=>{let reason='';if(w.durationMinutes>input.availableMinutes)reason='Exceeds available duration.';else if(w.environment!=='EITHER'&&input.environment!=='EITHER'&&w.environment!==input.environment)reason='Does not match the available setting.';else if(w.requiredEquipment.some(req=>req.includes('Outdoor')&&!input.equipment.some(x=>/outdoor|bicycle/i.test(x))))reason='Required equipment is unavailable.';else if((input.readiness.status==='Recovery recommended')&&w.demand>=4)reason='Current readiness excludes threshold and VO2 intensity.';else if(input.previousHighIntensity&&w.demand>=4)reason='Would create consecutive high-intensity cycling days.';else if(input.recoveryWeek&&w.demand>=5)reason='Recovery-week safety cap excludes this demand.';else if((input.nextQualityDays??9)<=1&&w.demand>=4)reason='Too close to the next quality session.';else if((input.weeklyLoadMinutes??0)>=600&&w.demand>=4)reason='Current weekly workload excludes additional intensity.';else if(input.recentLongOutdoor&&(w.discipline==='Endurance'||w.discipline==='Outdoor endurance')&&w.demand>=3)reason='Recent long outdoor riding already supplied this endurance workload; no bonus work is added.';else if(input.struggledWorkoutId&&workoutById(input.struggledWorkoutId)&&w.difficulty>(workoutById(input.struggledWorkoutId)!.difficulty)&&w.durationMinutes>workoutById(input.struggledWorkoutId)!.durationMinutes)reason='A struggled workout blocks simultaneous intensity and duration increases.';if(reason)rejected.push({workout:w,reason});return !reason});const scored=valid.map(w=>{const zone=input.development.find(z=>z.zone===w.primaryZone),score=(w.purpose.toLowerCase().includes(input.purpose.toLowerCase().split(' ')[0])?SELECTION_WEIGHTS.purposeMatch:0)+(w.discipline.toLowerCase().includes(input.goal.toLowerCase().split(' ')[0])?SELECTION_WEIGHTS.goalMatch:0)+(zone?.level?Math.max(0,SELECTION_WEIGHTS.levelFit-Math.abs(w.difficulty-zone.level)*3):0)+Math.max(0,SELECTION_WEIGHTS.durationFit-Math.abs(w.durationMinutes-input.availableMinutes)/10)+(w.environment===input.environment||w.environment==='EITHER'?SELECTION_WEIGHTS.environmentFit:0)+(input.recoveryWeek?(w.demand<=3?SELECTION_WEIGHTS.phaseFit:0):SELECTION_WEIGHTS.phaseFit)+(input.readiness.status==='Proceed with control'&&w.demand<=3?SELECTION_WEIGHTS.recoveryFit:0)+((input.weeklyLoadMinutes??0)<600?SELECTION_WEIGHTS.workloadFit:0)+((input.nextQualityDays??9)>1||w.demand<=3?SELECTION_WEIGHTS.proximityFit:0)+(input.recentLongOutdoor&&w.demand<=2?SELECTION_WEIGHTS.outdoorReplacement:0);return {w,score}}).sort((a,b)=>b.score-a.score||a.w.id.localeCompare(b.w.id));const selected=scored[0]?.w??null;for(const candidate of scored.slice(1,4))rejected.push({workout:candidate.w,reason:`Ranked below ${selected?.title??'the selected session'} on purpose, level, duration and setting fit.`});return {selected,selectedReason:selected?`${selected.title} best satisfies the hard safety constraints, then scores highest using the documented purpose, goal, level, duration, setting, phase and recovery weights.`:'No curated workout satisfies every hard safety constraint.',rejected:rejected.slice(0,3)}}
export function fourWeekPreview(plan:TrainingPlan,startWeek=1,generatedAt=new Date().toISOString()):FourWeekPreview{const weeks=plan.weeks.slice(startWeek-1,startWeek+3),a=weeks.flatMap(w=>w.assignments),cycling=a.filter(x=>x.type==='CYCLING'||x.type==='ASSESSMENT'),zoneExposure:Partial<Record<TrainingZone,number>>={};for(const x of cycling){const z=assignmentWorkout(x)?.primaryZone??'Endurance';zoneExposure[z]=(zoneExposure[z]??0)+x.durationMinutes}const conflicts:string[]=[];for(const w of weeks){const fri=w.assignments[4],sat=w.assignments[5];if(fri?.demandingCycling&&sat?.durationMinutes>=75)conflicts.push(`Week ${w.number}: quality work immediately precedes Saturday’s long ride.`)}return {generatedAt,fromWeek:startWeek,totalRideMinutes:cycling.reduce((n,x)=>n+x.durationMinutes,0),strengthSessions:a.filter(x=>x.type==='STRENGTH').length,qualitySessions:cycling.filter(x=>x.demandingCycling).length,enduranceSessions:cycling.filter(x=>!x.demandingCycling).length,outdoorSessions:cycling.filter(x=>x.environment==='OUTDOOR').length,recoveryDays:a.filter(x=>x.intensity==='RECOVERY').length,recoveryWeeks:weeks.filter(x=>x.recoveryWeek).length,zoneExposure,sustainedClimbingSessions:cycling.filter(x=>/climb/i.test(`${x.title} ${x.purpose}`)).length,consecutiveTrainingDays:Math.max(...weeks.map(w=>maxConsecutive(w.assignments))),highDemandDays:cycling.filter(x=>x.intensity==='HARD').length,conflicts,historyFingerprint:a.filter(x=>x.status!=='PLANNED').map(x=>`${x.id}:${x.status}`).join('|')}}
const maxConsecutive=(a:CalendarAssignment[])=>{let max=0,n=0;for(const x of a){n=x.durationMinutes>0&&x.type!=='REST'?n+1:0;max=Math.max(max,n)}return max}
export function assignmentWorkout(a:CalendarAssignment){if(a.environment==='OUTDOOR')return workoutById(a.durationMinutes>=90?'outdoor-endurance-120':'outdoor-easy-60');if(/VO2/i.test(a.title))return workoutById('vo2-5x4');if(/FTP|threshold/i.test(a.title))return workoutById('threshold-4x8');if(/climb/i.test(`${a.title} ${a.purpose}`))return workoutById('climbing-sustain-60');if(a.intensity==='RECOVERY')return workoutById('recovery-spin-30');return workoutById(a.durationMinutes>=60?'endurance-steady-75':'endurance-steady-45')}
export function replaceWithAlternative(plan:TrainingPlan,assignmentId:string,replacement:CuratedWorkout,record:AdaptationRecord):{plan:TrainingPlan;record:AdaptationRecord}{return {plan:{...plan,weeks:plan.weeks.map(w=>({...w,assignments:w.assignments.map(a=>a.id===assignmentId?{...a,workoutId:replacement.id,title:replacement.title,purpose:replacement.purpose,durationMinutes:replacement.durationMinutes,environment:replacement.environment==='EITHER'?'FLEXIBLE':replacement.environment,intensity:replacement.demand>=4?'HARD':replacement.demand===1?'RECOVERY':'EASY',demandingCycling:replacement.demand>=4,substitution:{kind:'REPLACE',reason:record.explanation,replacementId:replacement.id},status:'REPLACED'}:a)}))},record}}
export function readinessSafeguard(workout:CuratedWorkout,readiness:ReadinessProjection){if(readiness.stale)return {workout,changed:false,reason:'Today’s health information is stale, so RtR did not use it to change the workout.'};if(readiness.status==='Recovery recommended'&&workout.demand>=4)return {workout:workoutById('recovery-spin-30')!,changed:true,reason:'Current readiness recommends recovery, so intensity is replaced and the original remains in adaptation history.'};return {workout,changed:false,reason:'Current readiness does not require a workout change.'}}
export function jeanAdaptiveExplanation(original:CuratedWorkout,recommended:CuratedWorkout,evidence:string[],unavailable:string[],weekEffect:string){return `Originally planned: ${original.title}. Recommended now: ${recommended.title}. Why: ${recommended.purpose} Evidence used: ${evidence.join('; ')||'none'}. Not available: ${unavailable.join('; ')||'none'}. Rest of week: ${weekEffect}`}
export function outdoorEvidence(activity:OutdoorActivity){return {date:activity.date,durationMinutes:Math.round(activity.durationSeconds/60),powerWatts:activity.powerWatts??null,source:activity.source}}
export function hydrateAdaptive(state:Alpha4025State):AdaptiveTrainingState{return {...emptyAdaptiveTraining(),...state.adaptive,metadataVersion:WORKOUT_METADATA_VERSION,selectedAlternatives:{...state.adaptive?.selectedAlternatives}}}
export function halInitialZoneDevelopment():ZoneDevelopment[]{return ALL_ZONES.map(zone=>zone==='Endurance'?{zone,level:2.5,confidence:'Low',lastEvidenceDate:'2026-09-19',evidenceSource:'Outdoor baseline: duration, heart rate, distance and reported elevation; no power',successfulWorkoutIds:['hal-outdoor-baseline-2026-09-19'],struggledWorkoutIds:[],direction:'Stable'}:{zone,level:null,confidence:'None',lastEvidenceDate:null,evidenceSource:'Insufficient completed workout evidence',successfulWorkoutIds:[],struggledWorkoutIds:[],direction:'Insufficient evidence'})}
export type ExecutableWorkoutSection={id:string;title:string;durationSeconds:number;zone:TrainingZone;ftpRange?:[number,number];rpe:string;cadence:[number,number];resistance:string;jean:string}
export function workoutSections(workout:CuratedWorkout):ExecutableWorkoutSection[]{
 const intervalSeconds=workout.repetitions*(workout.workIntervalMinutes+workout.recoveryIntervalMinutes)*60,remaining=Math.max(0,workout.durationMinutes*60-intervalSeconds),warmup=Math.floor(remaining/2),cooldown=remaining-warmup,sections:ExecutableWorkoutSection[]=[]
 if(warmup)sections.push({id:`${workout.id}-warmup`,title:'Progressive warm-up',durationSeconds:warmup,zone:'Recovery',ftpRange:[.45,.65],rpe:'Very easy to easy',cadence:[80,92],resistance:'Light, gradually supported resistance',jean:'Settle into smooth breathing and controlled pedal speed.'})
 for(let index=0;index<workout.repetitions;index++){if(workout.workIntervalMinutes)sections.push({id:`${workout.id}-work-${index+1}`,title:`${workout.title} · Work ${index+1}`,durationSeconds:workout.workIntervalMinutes*60,zone:workout.primaryZone,ftpRange:workout.ftpRange,rpe:workout.unknownFtpEffort,cadence:workout.cadence,resistance:workout.resistanceGuidance,jean:`Hold the ${workout.primaryZone.toLowerCase()} purpose with controlled form.`});if(workout.recoveryIntervalMinutes)sections.push({id:`${workout.id}-recovery-${index+1}`,title:`Recovery ${index+1}`,durationSeconds:workout.recoveryIntervalMinutes*60,zone:'Recovery',ftpRange:[.4,.6],rpe:'Very easy',cadence:[78,92],resistance:'Reduce load and keep moving comfortably',jean:'Ease the load and let your breathing settle.'})}
 if(cooldown)sections.push({id:`${workout.id}-cooldown`,title:'Cooldown',durationSeconds:cooldown,zone:'Recovery',ftpRange:[.4,.55],rpe:'Very easy',cadence:[78,90],resistance:'Light recovery resistance',jean:'Finish easier than you started and note how the work felt.'})
 return sections
}
export function validExecutableWorkout(workout:CuratedWorkout){const sections=workoutSections(workout);return sections.length>0&&sections.reduce((sum,s)=>sum+s.durationSeconds,0)===workout.durationMinutes*60&&sections.every(s=>s.id&&s.title&&s.durationSeconds>0&&s.zone&&s.rpe&&s.cadence[0]>0&&s.resistance&&s.jean&&!/attack|peloton|KOM|race|sprint/i.test(s.jean))}
export function recordZoneEvidence(profile:ZoneDevelopment[],workoutId:string,date:string,status:'SUCCESS'|'STRUGGLED'|'INCOMPLETE',source='Completed off-season assignment'):ZoneDevelopment[]{const workout=workoutById(workoutId);if(!workout)return profile;return profile.map((zone):ZoneDevelopment=>{if(zone.zone!==workout.primaryZone&&zone.zone!==workout.secondaryZone)return zone;const successful=status==='SUCCESS'?Array.from(new Set([...zone.successfulWorkoutIds,workoutId])):zone.successfulWorkoutIds,struggled=status==='SUCCESS'?zone.struggledWorkoutIds:Array.from(new Set([...zone.struggledWorkoutIds,workoutId])),level=status==='SUCCESS'?Math.min(10,Math.max(zone.level??1,workout.difficulty)):Math.max(1,(zone.level??workout.difficulty)-.5);return {...zone,level,confidence:successful.length+struggled.length>=4?'High':successful.length+struggled.length>=2?'Moderate':'Low',lastEvidenceDate:date,evidenceSource:source,successfulWorkoutIds:successful,struggledWorkoutIds:struggled,direction:status==='SUCCESS'?'Building':'Recovering'}})}
