import type { LivePrescription } from './terrainModifier.ts'
/** Preview and activation consume the exact same resolver output object. */
export function targetPreview4023(name:string,duration:number,prescription:LivePrescription,sectionIndex=0){return {name,remaining:duration,power:prescription.power,cadence:prescription.cadence,resistance:prescription.resistance.replace(/ · START \d+% @ \d+ rpm| · Start \d+%/i,''),openingResistance:prescription.manualTarget.recommendedResistance,authoredId:`section-${sectionIndex}-${name}`,resolvedId:`${prescription.prescriptionId}|${prescription.manualTarget.calibrationConfidence}|${prescription.authoritativeGradient}`,zone:prescription.zone}}
